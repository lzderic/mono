import React from 'react';

const VehicleModelUpdate = () => {
    return(
        <main className="container font-text">
            <h2 className="title">Update vehicle model</h2>
        </main>
    );
}

export default VehicleModelUpdate;